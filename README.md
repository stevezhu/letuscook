# monorepo-template

Template for monorepo using [Turborepo](https://turbo.build/repo).

## Structure

- `packages/example-app` - Shared package used by `apps/example-desktop` and `apps/example-web`
- `packages/example-app-mobile` - Mobile-specific package used by `apps/example-mobile`
