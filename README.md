# monorepo-template

Template for monorepo using [Turborepo](https://turbo.build/repo).

## Structure

- `packages/example-app` - Shared package used by `apps/assistant-desktop` and `apps/assistant-web`
- `packages/example-app-mobile` - Mobile-specific package used by `apps/assistant-mobile`
